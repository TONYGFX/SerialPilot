# SerialPilot v0.1.2

这是面向真实硬件测试的 Windows 桌面补丁版本。

## 主要变化

- 串口下拉框显示端口号和系统设备描述，例如 `COM3 · USB-SERIAL CH340`。
- 实际连接仍使用稳定的端口号，不受显示名称影响。
- 真实串口适配器继续作为桌面程序和 MCP 服务的默认运行路径。
- Mock 适配器仅编译进 Rust 自动化测试，不进入发行版运行路径。

## 下载

普通用户下载 `serialpilot.exe` 即可运行；MCP 客户端只有在需要独立 MCP 进程时才使用对应的 MCP 可执行文件。
