{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the SerialPilot desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:allow-listen", "dialog:default", "opener:default"]
}
